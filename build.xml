<?xml version="1.0"?>
<project name="HoneyPot" default="dist" basedir=".">

	<property name="src.home" location="src" />
	<property name="build.home" location="bin" />
	<property name="config.home" location="src" />
	<property name="dist.home" location="dist" />
	<property name="jarfile" location="${dist.home}/Honeypot.jar" />

	<target name="prepare">
		<mkdir dir="${build.home}" />
		<mkdir dir="${dist.home}" />
	</target>

	<target name="clean" description="Remove all generated files.">
		<delete dir="${build.home}" />
		<delete dir="${dist.home}" />
	</target>

	<target name="compile" depends="prepare" description="Compile all source code">
		<javac srcdir="${src.home}" destdir="${build.home}" />
	</target>

	<target name="dist" depends="compile" description="Generates distribution jar file">
                <copy todir="${build.home}">
                        <fileset dir="${config.home}">
                                <include name="**/*.yml"/>
                        </fileset>
                </copy>

		<jar jarfile="${jarfile}" basedir="${build.home}" />
	</target>
</project>